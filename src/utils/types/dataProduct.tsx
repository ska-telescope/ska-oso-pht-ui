type DataProduct = {
  id: number;
  observatoryDataProduct: number;
  observations: string;
  pipeline: number;
  imageSize: string;
  pixelSize: string;
  weighting: string;
};

export default DataProduct;
